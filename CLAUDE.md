# CLAUDE.md

## 하네스: 온라인 ERD 작성 서비스

**목표:** React + Node.js 기반 ERD 다이어그램 작성 서비스를 에이전트 팀이 협력하여 설계·구현한다.

**트리거:** ERD 서비스 개발, 구축, 구현, 설계, 수정, 보완 등 ERD 서비스 관련 작업 요청 시 `erd-orchestrator` 스킬을 사용하라. 단순 질문(ERD란 무엇인가, 특정 기술 설명 등)은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-06 | 초기 구성 | 전체 | ERD 서비스 하네스 신규 구축 |
| 2026-06-07 | 관계선 연결점 분산 렌더링 | 프론트엔드 | 한 엔티티에 관계가 여러 개일 때 관계선 시작점이 동일해 겹쳐 보이는 문제 개선 — 플로팅 엣지 + 슬롯 분산 방식 도입 (`src/utils/edgeConnection.ts` 신규, `RelationshipEdge.tsx` 수정, `verify_fanout.mjs` e2e 추가) |
| 2026-06-07 | DataModeler Pro 디자인 적용 | 프론트엔드 | 사용자 제공 HTML 시안 기반 전면 재디자인 — M3 다크 팔레트 토큰화(`index.css` @theme), TopNav/좌측 사이드바(`Sidebar.tsx` 신규)/우측 Properties 패널/엔티티 노드/플로팅 줌 툴바 적용. 기존 기능(엔티티·컬럼 CRUD, 관계 연결, 저장/불러오기, 색상) 전부 유지, 시안의 미구현 요소(File/Edit/Export 메뉴, Relations/Layers/History, Description, Cancel/Apply)는 placeholder 또는 제외 (`verify_design.mjs` e2e 추가) |
| 2026-06-07 | 논리명/물리명 동시 표시 + Description | 프론트엔드 | 엔티티·컬럼에 논리명(한글 명칭) 필드, 엔티티에 Description 필드 추가 — 노드/사이드바/Properties 패널에 물리명+논리명 병기, 식별 관계 FK 자동 생성 시 논리명도 자동 조합("사용자 아이디"). optional 필드라 기존 저장 파일과 호환 (`types/erd.ts`, `erdStore.ts`, `EntityNode.tsx`, `EntityEditPanel.tsx`, `Sidebar.tsx` 수정, `verify_logical.mjs` e2e 추가) |
| 2026-06-07 | 유지보수 모드 QA 절차 의무화 | erd-orchestrator 스킬 | 실코드(erd-service/) 단계의 검증 절차 부재 — Phase 0에 유지보수 모드 분기 추가, 빌드+e2e+회귀+시각확인+이력기록 필수 체크리스트 신설 |
| 2026-06-07 | Undo/Redo·PNG 내보내기·자동 정렬·관계 타입 변경 | 프론트엔드 | 편집 기능 4종 추가 — ① 히스토리 스택 기반 Undo/Redo(Ctrl+Z/Y, 연속 입력 800ms 병합, 상한 50), ② html-to-image 기반 PNG 내보내기, ③ dagre 기반 관계 구조 자동 정렬(Undo 가능), ④ 엣지 선택 툴바에서 관계 타입 변경(식별↔비식별 전환 시 FK 자동 추가/제거). `erdStore.ts` 히스토리 재구성, `autoLayout.ts`·`exportImage.ts` 신규, `verify_features.mjs` e2e 14항목 PASS, 회귀(verify_logical) PASS |
| 2026-06-07 | 좌측 사이드바 placeholder 메뉴 제거 | 프론트엔드 | 사용하지 않는 Entities/Relations/Layers/History 4개 비활성 메뉴 제거 — Entity List가 사이드바 상단으로 이동, Help/Docs 푸터는 유지 (`Sidebar.tsx` 수정, NavItem 단순화, `verify_sidebar_cleanup.mjs` e2e 9항목 PASS, 회귀 verify_logical PASS) |
| 2026-06-07 | 상단 GNB placeholder 메뉴·범례 제거 | 프론트엔드 | 사용하지 않는 File/Edit/View/Export 메뉴 4개와 식별/비식별/선택 Barker 범례 제거 — 브랜드·Undo/Redo·Save·불러오기·알림/설정/아바타는 유지 (`Toolbar.tsx` 수정, LegendItem 삭제, `verify_gnb_cleanup.mjs` e2e 14항목 PASS, 회귀 verify_design PASS) |
| 2026-06-07 | 프로덕션 Node 서버 추가 | 서버 | Ubuntu 배포용 단일 Node 프로세스 구성 — Fastify 기반 `server/index.js` 신규(dist 정적 서빙 + SPA fallback, /api/*는 404 JSON으로 제외해 향후 백엔드 라우트 추가 지점 확보), `npm start` 스크립트·fastify/@fastify/static 의존성 추가. 포트는 PORT 환경변수(기본 8080). `verify_server.mjs` e2e 8항목 PASS, 회귀(verify_sidebar_cleanup, 8080 대상) PASS |
| 2026-06-07 | package-lock 동기화 수정 | 의존성 | Ubuntu 서버 `npm ci` 실패(EUSAGE — `@emnapi/core`·`@emnapi/runtime` 락파일 누락) 해결 — Windows에서 생성된 락파일에 wasm32-wasi optional 패키지의 의존성 항목이 빠지는 npm 크로스 플랫폼 버그. 1차 락파일 재생성으로 부족해 2차로 `@emnapi/core`·`@emnapi/runtime@1.10.0`을 devDependencies에 명시 추가해 락파일 항목 강제 생성. 빌드 산출물 해시 동일(회귀 없음), `npm ci --dry-run` PASS |
| 2026-06-07 | 기술 스펙 문서 작성 | 문서 | Vite 템플릿 보일러플레이트였던 `erd-service/README.md`를 실제 구현 기준 기술 스펙으로 교체 — 스택, 시스템/디렉토리 구조, 데이터 모델·FK 규칙, API 명세, DB 스키마, 환경변수, 배포 절차, e2e 스크립트 목록 (코드 변경 없음) |
| 2026-06-07 | 컬럼 드래그 순서 변경 | 프론트엔드 | 엔티티 설정 패널(Properties) 컬럼 목록에 드래그 핸들(drag_indicator) 추가 — HTML5 DnD로 행 위에 드롭하면 기존 `moveColumn` 액션 호출(스토어 변경 없음), 드래그 중 반투명·드롭 대상 하이라이트, Undo 가능. `EntityEditPanel.tsx` 수정, `verify_column_drag.mjs` e2e 5항목 PASS, 회귀(verify_features·verify_logical) PASS |
| 2026-06-07 | 비식별 관계도 FK 생성 (식별자 미포함) | 프론트엔드 | 표준 ERD 의미론 정정 — 모든 관계 타입에서 FK 자동 생성하되 플래그 차등: 식별=PK 포함+NN, 비식별=PK 미포함+NN, 선택=PK 미포함+NULL 허용. 타입 변경 시 FK 제거/재생성 대신 플래그만 전환(컬럼명 수정 보존, FK 없는 기존 데이터는 생성), 관계 삭제 시 모든 타입에서 자동 FK 제거. `erdStore.ts`(fkFlagsFor 신설, buildFKColumns 파라미터화)·`RelTypeModal.tsx`(설명 문구) 수정, verify_features 기대값 갱신 14항목 PASS·verify_fk_cleanup 13항목으로 확장 PASS, 회귀(verify_logical·verify_backend) PASS. ※ verify_fanout도 barker처럼 개편 이전 버튼 텍스트를 쓰는 구버전임을 확인(BASE_URL 지원만 추가) |
| 2026-06-07 | 엔티티/관계 삭제 시 잔존 FK 정리 | 프론트엔드 | 상위 엔티티 삭제 후 하위 엔티티에 자동 생성 FK 컬럼이 남는 버그 수정 — `deleteEntity`가 삭제 대상을 참조(refEntityId)하는 FK 컬럼을 함께 제거, 같은 규칙으로 `deleteRelationship`도 식별 관계 삭제 시 FK 제거(타입 변경 식별→비식별과 동일 동작). 둘 다 Undo로 복원 가능 (`erdStore.ts` 수정, `verify_fk_cleanup.mjs` e2e 11항목 PASS, 회귀 verify_features·verify_backend PASS) |
| 2026-06-07 | 네이티브 다이얼로그 → 커스텀 모달 교체 | 프론트엔드 | window.confirm/prompt/alert 전부를 디자인 시스템 모달로 교체 — `dialogStore.ts`(Promise 기반 alert/confirm/prompt)·`DialogModal.tsx`(danger 변형, Enter/Esc, 빈 입력 방지) 신규. 다이어그램 저장 이름 입력·이름 변경·삭제 확인·전환/로그아웃 시 미저장 경고·불러오기/PNG/저장 실패 알림에 적용, 확인창 없던 엔티티 삭제에도 확인 모달 추가. `verify_backend.mjs` 24항목으로 확장 PASS, 회귀(verify_features 14항목) PASS. ※ verify_barker는 디자인 개편 이전 버튼 텍스트를 쓰는 구버전이라 개편 시점부터 동작 불가(이번 변경과 무관) |
| 2026-06-07 | SQL DDL 내보내기 (MySQL) | 프론트엔드 | 줌 툴바에 SQL 내보내기 버튼 추가 — 엔티티/컬럼/FK 메타를 MySQL 8.x DDL로 변환해 `.sql` 다운로드. 타입 매핑(UUID→CHAR(36), VARCHAR/CHAR/DECIMAL/FLOAT size 반영), 논리명·설명→COMMENT, PK/UNIQUE/NOT NULL, refEntityId별 복합 FK 제약(참조 소실 시 제약 생략), 부모 우선 토폴로지 정렬 + FOREIGN_KEY_CHECKS 래핑(순환 대비). `utils/exportSql.ts` 신규, `ERDCanvas.tsx` ZoomToolbar 수정, `verify_sql_export.mjs` e2e 14항목 PASS, 회귀(verify_features 14항목) PASS |
| 2026-06-08 | 관계 타입 선택 모달 관계선 미리보기 | 프론트엔드 | 관계 종류 선택/변경 모달에서 5개 타입 각 타이틀 오른쪽에 실제 관계선 모양 미리보기 SVG 추가 — `RelLinePreview` 컴포넌트가 `RelationshipEdge.tsx`의 렌더링 규칙을 그대로 축소 반영(부모 점선+자식 실선 바커선/OPTIONAL 전체 점선, 1:M 까마귀발·1:1 마커없음, 식별 관계 uid 막대). 구현 중 `type.endsWith('_IDENTIFYING')`가 `NON_IDENTIFYING`도 잡는 버그 발견·명시 비교로 수정. `RelTypeModal.tsx` 수정, `verify_reltype_preview.mjs` e2e 18항목 PASS, 회귀(verify_features 14항목) PASS, 빌드 PASS |
| 2026-06-08 | 관계선 연결 핸들 크기 확대 | 프론트엔드 | 엔티티 상하좌우 연결점(핸들)이 작아(10×10px) 클릭/드래그가 어려운 점 개선 — `EntityNode.tsx`의 `handleStyle` 10→16px. hover 표시·연결 동작 규칙은 그대로. `verify_handle_size.mjs` e2e(getComputedStyle로 줌 무관 측정, 8핸들·16×16 확인) 6항목 PASS, 회귀(verify_features 14항목, 관계 드래그 연결) PASS, 빌드 PASS |
| 2026-06-08 | 1:1 관계에 실선+실선·점선+점선 타입 추가 | 프론트엔드 | 1:M의 SOLID/OPTIONAL 패턴을 1:1에도 확장 — 새 타입 `ONE_TO_ONE_IDENTIFYING_SOLID`(전체 실선·식별·PK 포함+NN), `ONE_TO_ONE_OPTIONAL`(전체 점선·비식별·NULL 허용) 추가. `lineStyleFor`·`isIdentifyingType`에 SOLID 추가, `fkFlagsFor`의 NN 예외를 `isOptionalType`(1:M·1:1 OPTIONAL 공통)로 일반화. `RelTypeModal` 미리보기(isSolid/isOptional/isIdentifying)·설명 확장. `types/erd.ts`·`erdStore.ts`·`RelationshipEdge.tsx`·`RelTypeModal.tsx` 수정. `verify_reltype_preview.mjs` 8타입 33항목 PASS, 회귀 verify_features 14·verify_fk_cleanup 13 PASS, 빌드 PASS |
| 2026-06-08 | 관계 종류 선택 모달 가로 폭 축소 | 프론트엔드 | 모달이 최대 폭 제한 없이 화면 전체(1600px=100vw)를 차지하던 것을 약 1/3 축소 — `RelTypeModal` 패널에 `max-w-[66vw]` 추가(1600→1056px, 뷰포트 비례). 내용/미리보기/기능 변경 없음. `verify_reltype_preview.mjs`에 폭 검증 추가(25항목 PASS, 회귀 겸함), 빌드 PASS |
| 2026-06-08 | 관계 타입 추가: 1:M 식별자 상속(실선+실선) | 프론트엔드 | 사용자가 RELATIONSHIP_LABELS에 중복 키로 추가하려던 '전체 실선 식별 관계'를 새 타입 `ONE_TO_MANY_IDENTIFYING_SOLID`로 정식 추가(중복 키 해소). 기존 식별 관계(점선+실선)와 구분되는 전체 실선 렌더링 — `RelationshipEdge`에 `lineStyleFor`(solid/half/dashed) 도입, solid는 점선 베이스 없이 단일 실선. 식별 관계로 처리(isIdentifyingType·isOneToMany·isIdentifying에 추가 → FK가 PK 포함+NN). `RelTypeModal` 미리보기·설명, 사용자가 변경한 라벨 6종 반영. `types/erd.ts`·`erdStore.ts`·`RelationshipEdge.tsx`·`RelTypeModal.tsx` 수정. `verify_reltype_preview.mjs` 24항목(6타입 line수/점선유무 + SOLID 엣지 실선·FK PK) PASS, 회귀 verify_features 14·verify_fk_cleanup 13 PASS(라벨 변경 반영해 갱신), 빌드 PASS |
| 2026-06-08 | 관계선 미리보기 부모쪽 세로선 제거 | 프론트엔드 | 미리보기 그림 좌측의 부모쪽 세로 막대 제거 요청 반영 — `RelLinePreview`에서 해당 `<line>` 삭제. e2e 기대 line 수 갱신(6/5/4/3/2). `RelTypeModal.tsx`·`verify_reltype_preview.mjs` 수정, e2e 18항목 PASS, 회귀(verify_features 14항목) PASS, 빌드 PASS |
| 2026-06-08 | 좌측 사이드바 접기/펼치기 토글 | 프론트엔드 | ERD를 한눈에 넓게 보기 위해 좌측 패널 접기 기능 추가 — `Sidebar.tsx` 내부 `collapsed` 로컬 상태로, 펼침(280px) 헤더에 접기 버튼(`left_panel_close`)·접힘(48px 레일)에 펼치기 버튼(`left_panel_open`)+Add Entity 단축 아이콘. App flex 레이아웃이라 접으면 캔버스 자동 확장. data-testid(sidebar/sidebar-toggle)·data-collapsed·aria 후크 부여, 기존 기능 전부 유지. `verify_sidebar_toggle.mjs` e2e 15항목 PASS, 회귀(verify_sidebar_cleanup 9항목) PASS, 빌드 PASS |
| 2026-06-07 | 로그인 + 다이어그램 DB 저장 백엔드 | 백엔드+프론트엔드 | 아이디/비밀번호 가입·로그인(JWT httpOnly 쿠키, scrypt 해싱)과 사용자별 다이어그램 DB 저장(PostgreSQL JSONB, DATABASE_URL 미설정 시 pg-mem 폴백) 추가 — `server/db.js`·`auth-routes.js`·`diagram-routes.js` 신규, `src/api/client.ts`·`authStore.ts`·`diagramStore.ts`(dirty 추적)·`AuthModal.tsx`·`utils/erdData.ts` 신규, Toolbar(DB 저장 버튼·아바타 로그인/로그아웃)·Sidebar(내 다이어그램 목록: 전환/이름변경/삭제)·vite proxy 수정. 비로그인 사용·PNG 내보내기·JSON 파일 Save/불러오기는 기존 그대로 유지. `verify_backend.mjs` e2e 19항목 PASS, 회귀(verify_server·verify_gnb_cleanup·verify_features 14항목) PASS |
| 2026-06-09 | 배타적 서브타입(SubSet) | 프론트엔드 | 바커식 슈퍼타입/서브타입 표기 추가(IE 배타 아크 아님) — 슈퍼타입 엔티티 안에 이름 붙은 SubSet 영역을 만들고 그 안에 서브타입을 중첩 박스(헤더+고유 컬럼)로 렌더. 서브타입은 PK를 슈퍼타입과 공유하므로 고유 속성만 보유, MVP라 독립 관계 핸들은 없음(관계는 슈퍼타입 레벨). 배타/포함·완전/불완전 토글 + 구분자 이름. SQL은 단일 테이블 롤업으로 매핑(구분자 VARCHAR + 서브타입 컬럼 nullable 평탄화 + 컬럼명 충돌 시 `_서브타입명` 접미사 + 구분자 도메인 CHECK + 배타성 CHECK, 불완전이면 구분자 NULL 케이스 허용). `types/erd.ts`(Subtype·Entity.subset* 필드, 전부 optional이라 기존 저장 호환)·`erdStore.ts`(서브타입 CRUD 7종, 전부 Undo)·`EntityNode.tsx`(SubSet 영역)·`EntityEditPanel.tsx`(SubSet 관리 UI)·`exportSql.ts`(buildSubtypeDDL) 수정. `verify_subtype.mjs` e2e 19항목 PASS, 회귀(verify_features 14항목) PASS, 빌드 PASS |
| 2026-06-09 | 서브타입 속성명·타입/크기 전체 노출 (잘림 제거) | 프론트엔드 | 노드 SubSet 영역 서브타입 박스에서 ① 속성명·논리명·서브타입명의 `truncate`(말줄임표)·`max-w` 폭 제한 제거 → `whitespace-nowrap` 전체 노출, ② 데이터 타입/크기 잘림 수정 — 박스가 `flex-1`(두 박스 균등 분할)이라 내용이 길면 제 share를 넘어 오른쪽 타입 열이 흘러나가 잘리던 것을 `flex-none`(내용 폭)으로 변경해 각 박스가 자기 내용만큼 차지하도록(+`overflow-hidden` 제거, 둥근 모서리는 헤더 `rounded-t-md`로 유지). 이름 길이에 맞춰 박스/노드 가로 확장. `EntityNode.tsx` 수정(+`data-testid` subtype-col-name/-type), `verify_subtype.mjs`에 긴 속성명 비잘림(getComputedStyle) + 타입 열 박스 내 노출(boundingRect) 검증 추가 **22항목 PASS**, 회귀 verify_features 14 PASS, 빌드 PASS. ※ 우측 Properties 패널(고정 320px)의 컬럼 요약 줄은 편집기 특성상 요약 truncate 유지(펼치면 전체 편집) |
| 2026-06-11 | ERD 변형 로직 공유 모듈 추출 (erdOps) | 프론트엔드 | MCP 서버와 로직을 공유하기 위해 `erdStore.ts`의 ERD 변형 로직(엔티티/컬럼/관계 CRUD·FK 자동생성·연쇄 삭제·이름 충돌 처리)을 프레임워크 비의존 순수 함수 `src/core/erdOps.ts`로 추출. 스토어 액션은 `pushHistory` 후 `erdOps.fn(docOf(s), …)`을 호출하는 thin wrapper로 전환(히스토리·선택·dirty 추적 보존). 서브타입/위치/undo·redo는 UI 전용이라 인라인 유지. 동작 무변경 — 회귀 verify_fk_namedup·verify_fk_cleanup 13·verify_features 14·verify_subtype 22·verify_logical PASS, 빌드 PASS. ※ verify_features의 FK 전환 3항목은 이전 이름충돌 커밋(4a11a05) 이후 stale였던 것을 부모 PK명 분리(pid)로 정정 |
| 2026-06-11 | MCP 서버 — Claude Code에서 ERD 제어 | 신규(mcp) | 배포된 ERD 서비스를 MCP로 제어해 다이어그램을 생성·편집하는 stdio 서버 신규(`erd-service/mcp/`). 백엔드가 blob 단위 CRUD만 제공하므로 매 변형은 `GET blob→erdOps 적용→PUT blob`. 인증: 서비스 계정으로 `/api/auth/login` JWT 쿠키 캡처+401 재로그인(백엔드 변경 없음). 도구 15종(다이어그램 CRUD·엔티티/컬럼/관계 + FK 자동생성, 참조는 id/이름 모두 허용). `@modelcontextprotocol/sdk`+`zod`, tsx로 실행(공유 `../src/core/erdOps` 직접 import). `verify_mcp.mjs` e2e 24항목 PASS(MCP SDK 클라이언트로 stdio 기동→도구 호출→저장 blob FK 검증→브라우저 렌더 확인). 라이브 싱크는 범위 외(새로고침 필요). 사용자 대면 웹 UI 변경 아님 → manual.html 갱신 불요. `.mcp.json.example`·README 동봉 |
| 2026-06-11 | MCP 연결 가이드 페이지 + 사이드바 메뉴 | 프론트엔드 | 다른 사용자가 MCP를 연결할 수 있도록 가이드 추가 — 자체 완결형 `public/mcp-guide.html`(manual.html과 동일 M3 다크 팔레트, TOC+본문, 4단계 설치·계정·.mcp.json·재시작 + 도구목록·문제해결·주의사항, 배포 주소 `toughdev.cafe24.com:8080` 예시) 신규, 좌측 사이드바 푸터의 **Help 위**에 `MCP 연결 가이드` 메뉴(cable 아이콘, `/mcp-guide.html` 새 탭) 추가(`Sidebar.tsx`). manual.html·USER_MANUAL.md 사이드바 절에 하단 메뉴 설명 추가. `verify_mcp_guide.mjs` e2e 13항목 PASS(메뉴 존재·순서·새탭·페이지 서빙/렌더), 회귀 `verify_sidebar_cleanup`(Docs가 링크로 바뀐 뒤 stale였던 셀렉터 정정 + MCP 메뉴 검증 추가)·`verify_sidebar_toggle` PASS, 빌드 PASS |
| 2026-06-16 | 원클릭 MCP 연결 — 원격 HTTP 전송 + 개인 토큰(PAT) | 백엔드+프론트엔드+MCP | 사용자가 레포 클론·Node 설치·`.mcp.json` 수기편집·서비스계정 평문비번·재시작을 모두 거쳐야 했던 stdio 온보딩을 **명령 한 줄 복사**로 축소. ① 전송: 기존 Fastify를 `tsx`로 실행해 stdio MCP 서버를 `/mcp` 원격 StreamableHTTP 엔드포인트로 합쳐 호스팅(`mcp/src/httpServer.ts` 신규 — SDK·zod가 도구와 동일 사본으로 해석되게 mcp/src 안에 배치, 세션별 transport 맵). 단일 프로세스 배포 유지. ② 인증: `mcp_tokens` 테이블(sha256 해시만 저장)+`/api/mcp-tokens` 발급/조회/취소 라우트, `app.authenticateAny`가 `Bearer erdmcp_…`(PAT) 또는 JWT 허용. 요청마다 그 사용자의 단기 JWT를 `reqCtx`(AsyncLocalStorage)로 도구에 전달해 자기 `/api`를 호출 → 기존 사용자 격리 그대로(서비스계정 제거). `session.ts` currentId를 세션별로, `httpClient.ts`는 HTTP/stdio 2모드. ③ 프론트: 좌측 사이드바 **MCP 연결** 모달(`McpConnectModal.tsx`)에서 토큰 발급→토큰 박힌 `claude mcp add --transport http …` 한 줄 복사·`.mcp.json` 대안·토큰 취소. `mcp-guide.html`/manual 갱신(원격 토큰 방식 권장, stdio는 고급으로). 배포 견고화 위해 SDK·zod·tsx를 루트 deps로(단일 `npm install`). stdio 방식·기존 기능 전부 유지. `verify_mcp_remote.mjs` 19항목 PASS(PAT 연결·FK·식별승격·사용자격리·잘못된토큰거부·브라우저렌더), `verify_mcp_connect_ui.mjs` 12항목 PASS, 회귀 `verify_mcp`(stdio 26)·`verify_backend`(24) PASS, 빌드 PASS, 스크린샷 확인(ss_mcp_remote·ss_mcp_connect·ss_mcp_guide_remote) |
| 2026-06-18 | 관계선 좌/우 절반 독립 편집 (우측 패널) | 프론트엔드 | 관계선 클릭 시 뜨던 선 위 작은 팝업(라벨+✎+×)을 제거하고, 우측 속성 패널에서 선의 좌(부모)·우(자식) 절반을 독립 설정하는 편집기로 일원화 — 8개 enum이 묶어 인코딩하던 카디널리티·식별·선스타일을 per-side로 분해해 "부모 실선+자식 점선" 등 기존 enum에 없던 조합까지 표현 가능. 데이터 모델은 **가산적**으로: `Relationship`에 optional `parentOptional`/`childOptional`/`childCardinality`/`identifying` 추가(기존 저장 파일 호환), `type`은 호환/생성 프리셋용으로 유지. 새 순수 모듈 `core/relationshipSides.ts`(deriveSides=명시필드 우선·없으면 type 파생, applySides=명시필드 갱신+근접 type 재계산+**자식 점선(선택)이면 식별 강제 해제 불변식** — 식별 FK는 PK·NOT NULL이라 NULL 허용과 양립 불가, fkFlagsForSides)에 렌더·FK의 단일 진실 원천을 둠. `RelationshipEdge`는 절반별 dashed/solid 오버레이로 4조합 렌더(양쪽 실선이면 단일 solid path로 단축 → SOLID 무회귀). `erdStore`에 `selectedEdgeId`/`selectEdge`(엔티티와 배타)·`updateRelationshipSides`(undo) 추가, `editingRelId`/`setEditingRel` 제거. `ERDCanvas` onEdgeClick/onPaneClick 배선·편집 모달 제거. 신규 `RelationshipEditPanel.tsx`(부모/자식 절반 레이블에 좌/우 대신 **실제 엔티티명** 표시·참여 토글·카디널리티·식별 체크(자식 점선이면 비활성)·미리보기·삭제), App 우측 슬롯 조건 렌더. 생성용 `RelTypeModal`(8 프리셋)·SQL/MCP/직렬화는 무변경(컬럼 플래그 기반·optional 필드 투명 통과). `verify_rel_sidepanel.mjs` e2e 25항목 PASS(패널 전환·배타선택·4조합 dasharray·까마귀발·식별 FK 승격·**자식 점선→식별 자동해제·체크박스 비활성**·삭제·undo/redo), 회귀 verify_features 13·verify_reltype_preview(SOLID FK 단언이 이전 이름충돌 커밋 이후 stale였던 것을 부모 PK명 분리로 정정)·verify_fk_cleanup·verify_fk_namedup·verify_logical·verify_subtype 22 PASS, 빌드 PASS, 스크린샷 확인(ss_rel_sidepanel·ss_rel_newcombo). manual.html·USER_MANUAL.md 관계 편집 절 갱신 |
| 2026-06-18 | 관계선 선택 시 강조 색상/굵기 개선 | 프론트엔드 | 선택된 관계선이 알아보기 어렵던 문제 개선 — ① `EDGE_SELECTED` 색상을 연한 `#c0c1ff`에서 채도 높은 `#a78bfa`(violet-400)로 교체, ② 선택 시 strokeWidth 1.5→2.5px로 굵게, ③ `CrowsFoot`·`OneTargetMarker`가 `EDGE_COLOR`를 직접 참조해 선택해도 마커 색이 바뀌지 않던 버그 수정 — `color` prop 전달로 마커도 함께 강조. 빌드 PASS, `verify_rel_sidepanel.mjs` 25항목 PASS(회귀 겸함) |
| 2026-06-18 | 재귀(자기 참조) 관계 지원 | 프론트엔드 | 엔티티가 자기 자신과 관계를 맺는 재귀 구조 추가 — ① `ERDCanvas` `source === target` 차단 제거, ② `edgeConnection.ts`에 자기 참조 전용 좌표 계산(우측 상단 출발→하단 진입), ③ `RelationshipEdge`에 `selfLoopPath()` 함수(우하단 사각 bezier 루프), ④ `erdOps.buildFKColumns`에 `namePrefix` 파라미터 추가 — 자기 참조 FK를 `parent_id`처럼 접두사로 PK명 충돌 방지, ⑤ `autoLayout.ts`에서 자기 루프 엣지 dagre 제외. SQL 내보내기·관계 속성 패널·Undo는 별도 수정 없이 동작. `verify_self_ref.mjs` e2e 신규, 회귀 verify_features 13 PASS, 빌드 PASS |
| 2026-06-18 | 로그인 시 5초 자동 저장 | 프론트엔드 | 로그인 상태에서 기존 다이어그램(`currentId` 있음)에 변경이 생기면 5초마다 자동 DB 저장 — `diagramStore.autoSave()`(dirty·currentId·saving 조건 검사 후 silent `updateDiagram`, 실패는 무시 후 다음 인터벌 재시도) 신규, `App.tsx`에 `status === 'authed'`일 때 `setInterval(autoSave, 5000)` + cleanup. 새 다이어그램(currentId 없음)은 건너뜀(이름 입력 필요). 빌드 PASS, verify_features 13 PASS(회귀) |
| 2026-06-18 | 좌측 HELP 메뉴 제거 + Claude 프롬프트 가이드 | 프론트엔드 | 비기능 Help 메뉴 제거, MCP로 ERD를 그리기 위한 Claude 프롬프트 예시 페이지 추가 — `Sidebar.tsx` 푸터에서 Help NavItem 삭제 + `Claude 프롬프트 가이드`(smart_toy 아이콘, `/prompt-guide.html` 새 탭) 추가. `public/prompt-guide.html` 신규(M3 다크 팔레트, TOC+분류별 예시: 엔티티 생성·컬럼·관계·삭제·쇼핑몰/블로그/인사/재귀 시나리오·팁·데이터 타입 치트시트). 빌드 PASS, verify_features 13 PASS(회귀), 스크린샷 확인(ss_prompt_guide) |
| 2026-06-25 | 메모(스티키 노트) | 프론트엔드 | 캔버스 위 어디든 자유롭게 텍스트 메모를 배치하는 기능 추가 — ① 좌측 사이드바 Add Memo 버튼·캔버스 더블클릭으로 메모 생성, ② `MemoNode.tsx`(NodeResizer·6색 배경·헤더 색상 점 변경·× 삭제 버튼), ③ `MemoEditPanel.tsx`(우측 패널 텍스트 입력·색상 선택·삭제), ④ `types/erd.ts`(`Memo` 인터페이스·`MEMO_COLORS`·`ERDData.memos?`), ⑤ `core/erdOps.ts`(`addMemo`·`updateMemo`·`deleteMemo` 순수 함수), ⑥ `erdStore.ts`(Snapshot에 memos 포함·Undo 가능·`selectedMemoId`·엔티티/엣지 선택과 배타), ⑦ `ERDCanvas.tsx`(`memo` nodeType 등록·위치/크기 변경 분기·더블클릭 핸들러), ⑧ `erdData.ts`·`diagramStore.ts`·`fileIO.ts` 저장·불러오기에 memos 포함. React Flow pane이 상위 레이어라 Playwright `force:true` 클릭이 pane에 착지하는 문제 → MemoNode outer div에 `onClick stopPropagation` 추가 + e2e에서 `dispatchEvent('click')` 방식으로 해결. `verify_memo.mjs` e2e 16항목 PASS, 회귀 verify_features 13 PASS, 빌드 PASS, 스크린샷 확인(ss_memo) |
| 2026-06-29 | 관계선 끝점 드래그로 부착 위치 이동 | 프론트엔드 | 자동 라우팅만 되던 관계선의 끝점(source/target)을 사용자가 드래그해 **같은 엔티티 테두리의 다른 위치**로 옮기는 기능 추가(엔티티 자체를 바꾸는 재연결 아님). 부착 위치는 **면(side)+정규화 오프셋(0~1)** 으로 저장 — 절대좌표가 아니라 엔티티 이동·크기 변경에도 상대적으로 따라감. ① `types/erd.ts`(`EndpointAnchor`/`AnchorSide` + `Relationship.sourceAnchor?`/`targetAnchor?`, optional이라 기존 저장 파일 호환), ② `utils/edgeConnection.ts`(`anchorToPoint`/`pointToAnchor` 좌표 변환, `computeEdgeEndpoints`가 앵커 있으면 `sideOf`+`attachPoint` 자동계산 우회, `attachPoint`는 앵커된 끝을 슬롯 분산에서 제외해 자동 선이 고정점으로 흘러들지 않게), ③ `core/erdOps.ts`(`updateRelationshipAnchor` 순수 함수, null이면 앵커 제거=자동 복귀, FK 부수효과 없음), ④ `erdStore.ts`(thin wrapper, `pushHistory('relAnchor:id:end')` — Snapshot이 relationships를 담아 Undo 자동), ⑤ `RelationshipEdge.tsx`(선택+비자기참조 시 `EdgeLabelRenderer`로 끝점 핸들 2개, pointer 드래그→`screenToFlowPosition`→로컬 프리뷰, up시 1회 커밋, 3px 임계값, 더블클릭 리셋, 선택 하이라이트를 `selectedEdgeId`로 게이트). 마커(까마귀발/막대)는 `geo` 기반이라 자동으로 앵커 따라감. 직렬화(`erdData.ts`)는 relationships 평탄 통과라 자동 영속. self-loop는 MVP 범위 외(핸들 미표시). `verify_edge_anchor.mjs` e2e 15항목 PASS(핸들 노출·상/하단 면 이동·동일 엔티티 유지·FK 무변경·선택 게이트·재선택 영속·더블클릭 복귀·Undo/Redo·target 드래그), 회귀 verify_features 13·verify_rel_sidepanel 25·verify_self_ref 12 PASS, 빌드 PASS, 스크린샷 확인(ss_edge_anchor), manual.html·USER_MANUAL.md 6.4절 추가 |
| 2026-06-30 | MCP 연결 "다음에 쓰면 failed" 진단 + pg-mem 데이터 소실 방지 가드 | 서버+문서 | 원격 MCP가 재사용 시 `failed`로 뜨고 토큰을 매번 재발급해야 하던 문제 규명 — **토큰 만료가 원인 아님**(PAT는 `expires_at` 없는 영구 토큰). 진짜 원인은 운영 서버가 `DATABASE_URL` 미설정 → pg-mem 인메모리 폴백(`server/db.js`)이라 `pm2 restart`/재배포 때마다 `users`·`mcp_tokens`·`diagrams`가 전부 소실 → 이전 PAT 해시가 사라져 401 → Claude Code가 failed 표시(저장 다이어그램도 동반 소실). 재발 방지로 `server/index.js` 수정: ① `.env`를 프로덕션에서도 로드(기존 `NODE_ENV!=='production'` 게이트 제거 — pm2 plain node에서 env 누락 footgun 차단), ② 기동 시 DB 종류 배너 + pg-mem 경고, 프로덕션에서 영속 DB 없으면 **기동 거부**(`ALLOW_PGMEM=1`로 우회), ③ `/api/health`(`{ok, db:kind}`) 추가. README 배포 절차에 `DATABASE_URL` 필수 경고+health 확인 추가. 실제 해결(영속 Postgres 프로비저닝 + `DATABASE_URL`/`JWT_SECRET` 설정)은 cafe24 서버 작업이라 사용자 측 적용 필요. 검증: prod+무DB **기동거부(exit 1)**·`ALLOW_PGMEM=1` 기동·`/api/health=pg-mem`·pg-mem 배너 확인, 회귀 **verify_server 8 PASS**. ※ `verify_backend`의 "reload 후 캔버스 빈 상태" 1건은 **커밋본에서도 동일 실패**하는 기존 stale(localStorage 캔버스 복원 vs 빈 상태 기대 — 이번 변경과 무관). MCP 전송 stateless 전환(Part C)은 후속 선택 과제 |
| 2026-06-30 | 관계 종류 선택에서 1:1 실선+실선·점선+점선 제외 | 프론트엔드 | 의미가 모호한 두 1:1 프리셋(`ONE_TO_ONE_IDENTIFYING_SOLID`·`ONE_TO_ONE_OPTIONAL`)을 관계 종류 선택 모달에서 제거 — `RelTypeModal.tsx`에서 `HIDDEN_TYPES`로 필터(선택 메뉴 8→6종). enum·렌더링·`getTypeDesc`·`RelLinePreview`·per-side 편집(`RelationshipEditPanel`)은 **기존 저장 다이어그램 호환** 위해 그대로 유지(그 타입을 쓰는 데이터는 정상 렌더, 좌/우 절반 편집으로 여전히 표현 가능). `verify_reltype_preview.mjs` 기대값 6종으로 갱신(ALL PASS), 회귀 verify_features 13·verify_rel_sidepanel 25 PASS, 빌드 PASS, 스크린샷 확인(ss_reltype_preview — 6종 노출), manual.html·USER_MANUAL.md 6.2 표 갱신(8→6가지 + 제외 사유 note) |
| 2026-07-01 | 신규 엔티티 생성 위치를 기존 엔티티 클러스터 기준으로 조정 | 프론트엔드 | Add Entity 시 새 엔티티가 캔버스 고정 원점(80,80) 기준 4열 그리드에 배치되어, 기존 엔티티들을 다른 위치로 옮겨둔 뒤 추가하면 그 모델링과 무관한(원래 원점 근처) 곳에 생성되는 문제 수정 — `core/erdOps.ts`의 `nextEntityPosition`이 고정 원점 대신 **현 엔티티들의 위치 바운딩박스 좌상단**을 원점으로 삼아 동일한 4열 그리드를 이어서 배치하도록 변경(엔티티가 없으면 기존과 동일하게 (80,80)). 프론트 UI(Add Entity 버튼)와 MCP `add_entity` 도구 모두 이 함수를 공유해 함께 수정됨. `verify_entity_position.mjs` e2e 신규 5항목 PASS(엔티티를 화면 멀리로 드래그 후 추가 시 새 엔티티가 옮긴 위치 근처(그리드 한 칸 간격)에 생성됨을 zoom 보정 좌표로 검증 — 수정 전 코드로는 FAIL 재현 확인), 회귀 verify_features 13 PASS, 빌드 PASS, 스크린샷 확인(ss_entity_position) |
| 2026-07-01 | 브랜드명 EasyERD → EasyRD 전면 변경 | 프론트엔드+문서 | 서비스 브랜드 표기를 전부 치환 — UI(`index.html` 타이틀, `Toolbar.tsx` 로고, `index.css` 주석), SQL 내보내기 헤더 주석(`exportSql.ts` + 기존 샘플 `ss_*.sql`), 공개 가이드 페이지(`manual.html`·`prompt-guide.html`·`mcp-guide.html`, 프롬프트 예시의 소문자 `easyerd`도 `easyrd`로), 문서(`README.md`·`USER_MANUAL.md`), e2e 어서션(`verify_design.mjs`·`verify_gnb_cleanup.mjs`·`verify_server.mjs`)의 브랜드 텍스트 셀렉터. 로직·MCP 서버명 등 코드 식별자에는 "easyerd" 문자열이 없어 동작 변경 없음(표기만 치환). 빌드 PASS, 회귀 verify_gnb_cleanup 14항목·verify_features 13항목 PASS, 스크린샷으로 상단 바 "EasyRD" 표시 확인 |
| 2026-07-01 | pg-mem 기동 가드가 NODE_ENV 미설정 시 무력화되던 결함 수정 | 서버 | 6/30에 추가한 "영속 DB 없으면 기동 거부" 가드가 `NODE_ENV==='production'`을 전제로 했는데, 실제 배포(pm2)가 그 값을 세팅하지 않아 가드가 조용히 무력화되어 **MCP 연결 끊김(토큰 소실) 사고가 재발**함을 확인 — pm2 재시작마다 여전히 pg-mem으로 떠서 계정·MCP 토큰·다이어그램이 매번 삭제되고 있었음. `server/index.js`의 거부 조건에서 `NODE_ENV` 체크를 제거해 **영속 DB(DATABASE_URL) 없으면 항상 기동 거부**(우회는 `ALLOW_PGMEM=1`만)로 변경 — 사람이 부가 조건을 기억해야 가드가 켜지는 구조를 제거. 로컬/QA에서 pg-mem을 쓰려면 이제 `ALLOW_PGMEM=1 npm start` 필요(README 개발/배포 섹션 갱신). 검증: `ALLOW_PGMEM` 없이 기동 시 **exit 1 확인**(NODE_ENV 무관), `ALLOW_PGMEM=1` 기동 후 `/api/health`→`{"ok":true,"db":"pg-mem"}` 확인, 회귀 verify_server 8 PASS·verify_backend 24 PASS(기존 stale 1건은 6/30 문서화된 것과 동일, 무관), 빌드 PASS. 실제 cafe24 서버의 `DATABASE_URL` 설정은 여전히 사용자 측 조치 필요(코드 가드는 "설정 안 하면 조용히 방치"를 막을 뿐 대신 설정해주지 않음) |
| 2026-07-02 | 브랜드명 변경: EasyRD → YourERD | 프론트엔드+문서 | 서비스 브랜드명을 (전날 2026-07-01에 EasyERD→EasyRD로 이미 변경돼 있던 상태에서) YourERD로 전면 교체 — 상단 바 로고(`Toolbar.tsx`), 페이지 타이틀(`index.html`), 디자인 토큰 주석(`src/index.css`), SQL 내보내기 헤더 주석(`utils/exportSql.ts`), 3개 정적 가이드 페이지(`public/manual.html`·`mcp-guide.html`·`prompt-guide.html`, 소문자 예시 문구 "easyrd MCP가 연결됐어?"도 "yourerd"로 함께 변경), `README.md`·`USER_MANUAL.md`, 브랜드 텍스트를 단언하던 e2e 스크립트(`verify_design.mjs`·`verify_gnb_cleanup.mjs`·`verify_server.mjs`)까지 총 12개 파일. 과거 SQL 내보내기 샘플 산출물(`ss_export_result.sql`·`ss_subtype_export.sql`)은 전날 커밋에서 이미 EasyRD로 치환된 상태 그대로 유지(스크린샷과 동일한 성격의 시점 기록이라 추가 변경 안 함). 빌드 PASS, 회귀 verify_gnb_cleanup 14 PASS(브랜드 단언 YourERD로 갱신)·verify_features 13 PASS·verify_server 8 PASS(프로덕션 빌드 기준), 스크린샷 확인(앱 상단 바·manual.html·mcp-guide.html·prompt-guide.html 4곳 모두 YourERD 렌더 확인). ※ verify_design.mjs는 브랜드 단언(topbarBrand)은 PASS했으나 이후 관계 타입 버튼 텍스트가 개편 이전 라벨이라 실패하는 기존 stale(이번 변경과 무관, verify_barker/verify_fanout과 동일 부류). ※ 작업 당시 로컬 클론이 전날의 EasyRD 리네임 커밋(301e968, origin/main)을 pull하기 전이라 "EasyERD"가 소스라고 착각한 채 진행했으나, rebase로 origin을 반영하며 최종 텍스트는 정확히 YourERD로 수렴함(중간 경유지였던 EasyERD/EasyRD 표기는 결과물에 남지 않음) |
| 2026-07-02 | 배포 도메인 변경: toughdev.cafe24.com:8080 → yourerd.com (https) | 문서+MCP | 사용자가 신규 도메인 구매 후 서버(cafe24 VPS, 기존과 동일 IP)에 연결 — DNS는 정상이었으나 앱이 8080에만 리스닝하고 있어 80/443 기본 포트 접속이 거부되는 문제를 진단(nslookup+curl로 직접 확인). 사용자가 서버에 nginx 리버스 프록시(80/443→8080) + certbot(Let's Encrypt) SSL을 직접 설정 완료(`https://yourerd.com` 정상 응답 확인됨, 인증서 만료 2026-09-30·certbot.timer 자동갱신 등록됨). 이에 맞춰 리포지토리 내 하드코딩된 옛 배포 주소를 코드 레벨에서 교체 — ① `public/mcp-guide.html` 사용자 노출 연결 예시 3곳(bash 명령·`.mcp.json` JSON·고급 stdio env, 전부 `http://toughdev.cafe24.com:8080`→`https://yourerd.com`), ② `mcp/create_ecommerce_erd.mjs`(BASE_URL 하드코딩을 형제 스크립트 `create_blog_erd.mjs`와 동일하게 `process.env.ERD_BASE_URL ?? 'https://yourerd.com'`로 env 오버라이드 가능하게 통일), ③ `verify_mcp_guide.mjs` 배포 주소 단언 문자열 갱신. 두 차례 독립 Explore 조사(프론트/루트 vs 서버·mcp·문서)로 그 외 하드코딩(쿠키 domain·CORS 등)이 없음을 교차 확인. `dist/**` 빌드 산출물은 재빌드로 자동 갱신, `localhost:8080` 개발 기본값들과 CLAUDE.md 과거 이력 속 도메인 언급은 도메인과 무관/시점 기록이라 미변경. 빌드 PASS, `verify_mcp_guide.mjs`는 2026-06-18 Help 메뉴 제거 이후 stale해진 사이드바 단언(이번 변경과 무관) 때문에 스크립트 중간에 예외로 중단되어 전체 PASS는 못 봤으나, 가이드 페이지 HTML을 직접 fetch해 `yourerd.com` 3곳 존재·`toughdev.cafe24.com` 0건을 확인하고 스크린샷으로 렌더 확인함, 회귀 verify_features 13 PASS |
| 2026-07-02 | nginx+certbot 배포 가이드 문서 신규 | 문서 | 위 도메인 연결 작업(nginx 리버스 프록시 + certbot SSL) 과정에서 실제로 겪은 문제(사용 중인 nginx가 `sites-available` 디렉토리 없음, `sites-enabled` 심볼릭 링크 누락으로 welcome 페이지 노출 등)와 해결 절차를 재사용 가능한 런북으로 정리 — `erd-service/DEPLOY_SSL.md` 신규(설치→리버스 프록시→방화벽→certbot 발급→자동갱신 원리·전제조건→NODE_ENV=production 권장→트러블슈팅 표→`sites-available` 없을 때 `conf.d` 대안). 앱 코드가 아닌 서버 인프라 설정 문서라 `mcp/README.md`와 같은 성격으로 최상위에 배치, `README.md` "배포(Ubuntu+pm2)" 절 끝에 링크 추가. 코드 변경 없음 — 빌드 PASS(영향 없음 확인용), e2e 생략(기능 변경 아님) |
| 2026-07-02 | 엔티티 선택 후 키보드 Delete로 삭제 | 프론트엔드 | 엔티티를 선택한 상태에서 `Delete` 키를 누르면 기존 🗑 버튼과 동일한 확인 모달(`confirmDialog`) 후 삭제되도록 추가 — `App.tsx`의 기존 전역 Ctrl+Z/Y `keydown` 핸들러에 분기 추가(입력 필드 포커스 중 가드는 기존 로직 재사용), `EntityEditPanel.tsx`의 삭제 버튼과 동일한 `deleteEntity` 액션·메시지 재사용이라 관계선·FK 정리·Undo/Redo 동작 동일. 구현 중 `@xyflow/react`(React Flow) 기본값 `deleteKeyCode='Backspace'`가 스토어를 거치지 않고 로컬 노드/엣지만 지워 데이터와 어긋나는 잠재 버그를 발견 — `ERDCanvas.tsx`의 `<ReactFlow>`에 `deleteKeyCode={null}` 추가해 라이브러리 기본 삭제를 비활성화하고 앱 레벨 핸들러가 전담하도록 정리(요청 범위는 엔티티 Delete 키만이라 관계선/메모는 대상 아님, 각자 기존 버튼 삭제 UX 유지). `verify_entity_delete_key.mjs` e2e 신규 10항목 PASS(미선택 시 무동작·확인 모달·취소·입력 필드 포커스 중 미동작·확인 후 삭제·Undo/Redo·연결관계 함께 삭제), 회귀 verify_features 13·verify_rel_sidepanel 25 PASS(deleteKeyCode 변경이 드래그 연결·엣지 선택에 영향 없음 확인), 빌드 PASS, 스크린샷 확인(ss_entity_delete_key). manual.html·USER_MANUAL.md 4.3절·단축키 표에 `Delete` 키 추가 |
| 2026-07-02 | 관계선 선택 후 키보드 Delete로 삭제 | 프론트엔드 | 직전 엔티티 Delete 키 삭제와 동일한 패턴으로 관계선까지 확장 — `App.tsx`의 `Delete` 분기에 `selectedEdgeId` 케이스 추가, `RelationshipEditPanel.tsx`의 "관계 삭제" 버튼과 동일한 `deleteRelationship` 액션·확인 메시지("부모→자식 관계를 삭제할까요? 자동 생성된 FK 컬럼도 함께 제거됩니다") 재사용. `ERDCanvas.tsx`의 `deleteKeyCode={null}`는 엔티티/관계선 공통 적용이라 추가 변경 불필요. `verify_relationship_delete_key.mjs` e2e 신규 13항목 PASS(미선택 시 무동작·확인 모달·타이틀이 "관계 삭제"로 정확히 구분됨(엔티티 삭제 다이얼로그와 혼동 방지)·취소·확인 후 삭제+FK 정리·Undo/Redo·삭제 후 양쪽 엔티티는 유지). 작성 중 테스트 스크립트 자체 버그 발견·수정: 자동 정렬 없이 2개 엔티티만 있는 상태로 엣지 중간점을 클릭하면 좌표가 노드와 겹쳐 실제로는 엔티티가 선택되는데, 그 엔티티 삭제 확인 메시지에도 "관계"·"삭제" 단어가 우연히 포함돼 있어 느슨한 문자열 검증이 오탐(false pass)했었음 — verify_features.mjs의 기존 자동 정렬 선행 패턴을 적용하고 다이얼로그 타이틀 정확 일치로 검증 강화. 회귀 verify_entity_delete_key 10·verify_rel_sidepanel 25 PASS, 빌드 PASS, 스크린샷 확인(ss_relationship_delete_key). manual.html·USER_MANUAL.md 6.3절·단축키 표 갱신("엔티티 또는 관계선") |
| 2026-07-02 | 메모 선택 후 키보드 Delete로 삭제 | 프론트엔드 | 엔티티·관계선에 이어 메모까지 Delete 키 삭제 확장 — `App.tsx`의 `Delete` 분기에 `selectedMemoId` 케이스 추가, `MemoEditPanel.tsx`의 삭제 버튼과 동일한 `deleteMemo` 액션·확인 메시지("이 메모를 삭제할까요?") 재사용(삭제 후 `selectMemo(null)`도 동일하게 호출). `verify_memo_delete_key.mjs` e2e 신규 11항목 PASS(미선택 시 무동작·확인 모달·타이틀 "메모 삭제"·취소·textarea 포커스 중 미동작·확인 후 삭제+패널 닫힘·Undo/Redo). 메모 노드 선택은 React Flow pane이 상위 레이어라 `dispatchEvent('click')`으로 클릭(`verify_memo.mjs`의 기존 패턴 재사용). 회귀 verify_memo 16·verify_entity_delete_key 10·verify_relationship_delete_key 13 PASS, 빌드 PASS, 스크린샷 확인(ss_memo_delete_key). manual.html·USER_MANUAL.md 8.5절·단축키 표 갱신("엔티티·관계선·메모"). 이로써 캔버스의 3가지 선택 가능 요소(엔티티/관계선/메모) 모두 Delete 키 삭제 지원 완료 |
| 2026-07-02 | 실시간 협업 1단계: 링크 공유 + 함께 보기 | 백엔드+프론트엔드 | 소유자가 링크로 다이어그램을 공유해 여러 명이 실시간으로 함께 보는 기능 추가(2단계 계획 중 1단계 — 소유자가 유일 편집자, 링크 소지자는 로그인 후 읽기 전용 관전). **동기화 방식**: erdOps 순수 함수를 재사용하는 서버 오퍼레이션 릴레이(Yjs 아님 — FK 자동생성·연쇄삭제가 비가환 복합 변형이라 CRDT와 충돌·MCP/SQL 파괴). 소유자가 op 브로드캐스트 + 주기 스냅샷(백스톱), 뷰어는 `applyRemote`로 읽기전용 적용. Phase 1은 소유자가 기존 5초 autosave로 영속(서버 영속 권위는 2단계). **신규**: `core/opDispatch.ts`(op-name→erdOps, client·server 공유), `collab/{protocol,wsClient}.ts`, `store/{collabStore,shareStore,sharedSessionStore}.ts`, `components/share/ShareModal.tsx`(McpConnectModal 미러·clipboard 폴백), `server/{collab-hub,collab-auth,collab-routes,access-util,share-routes}.js`. **수정**: `db.js`(diagram_shares 테이블·sha256 토큰·soft revoke), `mcp-token-util.js`(erdshare_ 토큰), `index.js`(@fastify/websocket·collab/share 라우트 등록), `erdOps.ts`(create 함수에 명시 id 옵션 — 협업 피어 간 id 재현, 가산적), `erdStore.ts`(applyRemote·emitIfLive·loadData {silent}·readOnly 가드), `diagramStore.ts`(전환 시 collab 해제), `authStore.ts`(공유 진입 시 restore 스킵), `App.tsx`(조인 플로우 `/d/:token`·읽기전용 Delete 게이팅), `Toolbar/Sidebar/ERDCanvas`(공유 버튼·presence 아바타·읽기전용 게이팅). **접근제어**: 링크별 고정 역할(viewer/editor)·개별 폐기, `GET /api/shared/:token`(로그인 필요, `ALLOW_ANON_SHARE=1`로 익명 허용 전환 가능), 토큰↔다이어그램 바인딩(다른 다이어그램 접근 불가), 소유자 전용 라우트는 그대로 비공개. `@fastify/websocket` 의존성 추가. `verify_share.mjs` e2e 18항목 PASS(발급/폐기·익명 401·소유자 라우트 비공개·토큰 바인딩·읽기전용·실시간 반영·presence 2명), 회귀 verify_features 13·verify_rel_sidepanel 25·verify_memo 16 PASS·verify_backend 23/24(1건은 2026-06-30 문서화된 기존 stale "reload 후 빈 캔버스"로 무관), 빌드 PASS, 스크린샷 확인(ss_share_viewer·ss_share_owner). manual.html·USER_MANUAL.md 10.3절 추가. ※ 2단계(여러 명 동시 편집·서버 영속 권위·서브타입 op·genId FK 조율·MCP 룸 경유)는 후속 |
| 2026-07-03 | 서브타입에 관계선 연결(서브타입 스코프 지정) | 프론트엔드 | 배타적 서브타입(2026-06-09)이 MVP라 관계는 항상 슈퍼타입 레벨에서만 맺을 수 있었던 제약 해소 — "Car 서브타입만 Insurance를 가진다"처럼 특정 서브타입 전용 관계를 표현 가능해짐. 서브타입은 React Flow 노드가 아니라 `Entity.subtypes[]`의 값 객체(독립 좌표·Handle 없음)라 새 Handle/DOM 좌표 인프라 대신, 관계는 지금처럼 엔티티 레벨에서 생성하고 우측 `RelationshipEditPanel`에서 부모/자식 각 side별로 "구체적 대상"(서브타입) 드롭다운으로 사후 지정하는 방식 채택(`edgeConnection.ts`/`RelationshipEdge.tsx`/`EntityNode.tsx` 무변경 — 회귀 위험이 가장 큰 좌표 계산 영역을 아예 건드리지 않음, 시각적으로 서브타입 쪽에 붙이고 싶으면 기존 끝점 드래그(`EndpointAnchor`, 2026-06-29) 재사용). `types/erd.ts`에 `Relationship.sourceSubtypeId?`/`targetSubtypeId?`(둘 다 optional·가산적) 추가. 자식(target) 쪽 스코프 지정 시 자동 FK 컬럼이 `Entity.columns`가 아니라 그 서브타입의 `columns`로 이동 — `buildSubtypeDDL`의 기존 nullable 평탄화·컬럼명 충돌 회피·배타성 CHECK을 그대로 재사용해 정합성 확보(SQL 쪽은 `exportSql.ts`의 FK 제약 스캔·토폴로지 정렬만 서브타입 컬럼까지 포함하도록 확장, `buildSubtypeDDL` 자체는 무변경). 부모(source) 쪽 스코프는 라벨 표시용일 뿐 FK 생성엔 무영향(서브타입은 PK가 없어 항상 슈퍼타입의 실제 PK 참조). 새 불변식: 자식이 서브타입 전용이면 식별 관계 강제 해제(`relationshipSides.ts`의 `applySides` — 서브타입 컬럼은 조건부라 PK 불가). `erdOps.ts`에 FK 배치 대상을 결정하는 `fkHost`/`withFkHost` 헬퍼 신설 + `addRelationship`·`updateRelationshipType`·`updateRelationshipSides`·`deleteRelationship`·`deleteEntity`가 이를 경유하도록 리팩터, 신규 `updateRelationshipSubtypeScope`(스코프 변경 시 FK 컬럼을 이전 host에서 새 host로 이동)·`removeSubtypeCascade`(서브타입 삭제 시 target 스코프 관계는 삭제, source 스코프 관계는 스코프만 해제) 추가. `erdStore.ts`에 얇은 wrapper 액션 추가, `removeSubtype`이 캐스케이드 호출. `core/opDispatch.ts`에 새 op 등록(실시간 협업 반영, `collab/protocol.ts`는 op 이름/인자를 제네릭하게 다뤄 무변경). MCP는 서브타입 CRUD 자체가 노출돼 있지 않아 범위 밖(후속 과제). `verify_subtype_relationship.mjs` e2e 신규 26항목 PASS(자식/부모 스코프 드롭다운 노출 조건·FK 서브타입 박스 이동·식별 체크박스 강제비활성·SQL nullable+CHECK+FK제약·Undo/Redo·서브타입 삭제 캐스케이드 양쪽 케이스), 회귀 verify_subtype 22·verify_rel_sidepanel 25·verify_fk_cleanup 13·verify_features 13·verify_self_ref 12 PASS, 빌드 PASS, 스크린샷 확인(ss_subtype_relationship). manual.html·USER_MANUAL.md 6.5절 신규 추가 |
| 2026-07-03 | 서브타입 박스에서 직접 드래그로 관계 연결 | 프론트엔드 | 직전 작업(서브타입 관계 스코프 지정)을 "엔티티 테두리에서 연결 → 우측 패널에서 서브타입 사후 지정" 2단계로 구현했는데, 로컬에서 써본 사용자가 "바로 연결되기를 원했는데 불편하다"는 피드백 — 서브타입 박스에서 곧바로 드래그해 연결하는 지름길 추가(패널 방식은 사후 조정용으로 그대로 유지). 다시 검토하니 이미 `erdOps.addRelationship`에 `scope` 파라미터를 준비해뒀던 덕분에 좌표 계산(`edgeConnection.ts`/`RelationshipEdge.tsx`)은 무변경으로 끝남 — 선은 지금처럼 엔티티 레벨 floating edge로 그려지고, 서브타입 박스는 "연결 시작/종료 지점"만 제공. `EntityNode.tsx`(각 서브타입 박스에 `position:relative` + 축소 Handle 1개, id `sub:{subtypeId}` 인코딩), `ERDCanvas.tsx`(구현 중 기존 버그 발견·수정 — `onConnect`의 source/target 정규화가 `sourceHandle`/`targetHandle`은 함께 뒤집지 않아 어긋나고 있었음; `handleRelTypeSelect`가 handle id의 `sub:` 접두사를 파싱해 스코프를 만들어 `addRelationship`에 전달), `erdStore.ts`(`addRelationship` 액션에 `scope` 옵션 추가, `emit` 페이로드 포함 — `opDispatch.ts`는 이미 해당 인자를 받게 되어 있어 무변경). `verify_subtype_direct_connect.mjs` e2e 신규 12항목 PASS(서브타입 핸들 렌더·자식 스코프 즉시 FK 반영·식별 체크박스 즉시 비활성·드래그 시작이 서브타입 핸들일 때 부모 스코프까지 정확히 잡힘 — onConnect swap 버그 수정 검증 겸함), 회귀 verify_subtype_relationship 26·verify_rel_sidepanel 25·verify_self_ref 12 PASS, 빌드 PASS, 스크린샷 확인(ss_subtype_direct_connect). manual.html·USER_MANUAL.md 6.5절 갱신(방법 A/B 병기) |
| 2026-07-04 | 엔티티/메모 다중 선택(러버밴드 박스) + 그룹 이동/일괄 삭제 | 프론트엔드 | 사용자 요청 — 엔티티·메모가 한 번에 하나씩만 선택·이동 가능해 여러 개를 함께 옮기기 불편함. React Flow(`@xyflow/react`)가 Shift+드래그 박스 선택·그룹 드래그를 기본 제공한다는 걸 확인해 트리거 자체는 라이브러리 그대로 사용(별도 `panOnDrag`/`selectionKeyCode` 설정 불필요), 대신 이 앱 고유의 두 가지 구멍을 메움. ① **선택 유지**: 기존 `useEffect(() => setNodes(rfNodes), ...)`가 store 변경(위치 이동 등)마다 노드 배열을 통째로 새로 만들어 React Flow 내부 `selected` 플래그를 매번 지워버려, 그룹을 드래그해 옮기자마자 선택 표시가 사라지던 문제 — 재계산 시 이전 `selected` 값을 병합해 보존하도록 수정. ② **Undo 단위**: 여러 노드의 position 변경이 각각 별도 히스토리 스냅샷으로 쌓여 그룹 이동 하나를 되돌리려면 Undo를 여러 번 눌러야 했던 문제 — `updateNodePosition`/`updateMemoPosition`을 하나의 `moveNodes(entityMoves, memoMoves)`로 통합해 같은 드래그로 옮긴 항목 전부를 히스토리 1회로 묶음(단일 드래그도 동일 경로 사용, 회귀 없음). 우측 패널: React Flow의 `onSelectionChange`를 `erdStore`의 `selectedEntityIds`/`selectedMemoIds` 배열로 반영하는 `setSelection` 액션 신설(정확히 1개면 기존 `selectedEntityId`/`selectedMemoId`도 함께 갱신해 기존 단일 편집 패널과 호환), 2개 이상이면 새 `MultiSelectPanel.tsx`("N개 선택됨" + 일괄 삭제 버튼)가 기존 Entity/Relationship/Memo 편집 패널 대신 표시됨. 일괄 삭제는 `deleteMany(entityIds, memoIds)` 신설(히스토리 1회, `deleteEntity`/`deleteMemo`를 순차 적용해 연결 관계선·자동 FK 정리 로직 재사용) — `App.tsx`의 기존 `Delete` 키 핸들러에 다중 선택 분기 추가(확인 다이얼로그 "일괄 삭제"). 구현 중 발견한 부수 버그: `MemoNode.tsx`가 z-index 문제로 자체 `onClick`에서 `stopPropagation` 후 `selectMemo`를 직접 호출하던 기존 방식이, 메모가 포함된 다중 선택 상태에서 그 메모를 단순 클릭(드래그 아님)하면 React Flow 내부 선택은 그대로인데 우리 쪽 다중 선택 배열만 단일로 좁혀져 어긋나는 문제 — 이미 다중 선택에 포함된 메모 클릭은 무시하도록 가드 추가. `types/erd.ts`·SQL·MCP·협업 op 변경 없음(순수 UI 선택 상태 + 기존 delete/position 액션 재구성). `verify_multiselect.mjs` e2e 신규 23항목 PASS(박스 선택 3개·그룹 드래그 동일 델타 이동·드래그 후 선택 유지·Undo 1회로 그룹 이동 전체 복원·Delete 확인 다이얼로그(취소/확인)·일괄 삭제 Undo/Redo 1회·빈 캔버스 클릭으로 전체 해제·해제 후 개별 클릭 정상 동작), 회귀 verify_features 13·verify_entity_delete_key 10·verify_relationship_delete_key 13·verify_memo_delete_key 11·verify_memo 16·verify_entity_position 5·verify_rel_sidepanel 25·verify_subtype 22·verify_self_ref 12·verify_edge_anchor 15 전부 PASS, 빌드 PASS, 스크린샷 확인(ss_multiselect_boxselect·ss_multiselect_groupdrag·ss_multiselect_final). manual.html·USER_MANUAL.md 9.4절 신규 + 단축키 표 갱신 |
| 2026-07-05 | 내 다이어그램 목록 — 복제(Duplicate) 기능 | 프론트엔드 | 사용자 요청 — 기존 이름 변경(✎)·삭제(🗑) 아이콘 옆에 복제(⧉) 아이콘 추가. 백엔드 변경 없이 기존 `api.getDiagram`(원본 data 조회)+`api.createDiagram`(새 이름으로 생성) 두 엔드포인트 조합만으로 구현(이미 `saveCurrent()`가 신규 저장 시 쓰던 조합과 동일) — 다이어그램 JSON이 작고 두 라우트가 이미 충분히 검증돼 있어 서버 쪽 원자적 `POST /:id/duplicate` 라우트 신설은 보류. `diagramStore.ts`에 `duplicate(id)` 액션 신설(`rename`과 동일하게 `promptDialog`로 이름 입력, 기본값 "{원본 이름} 사본", `saveCurrent`처럼 실패 시 `alertDialog`로 안내) — `rename`/`remove`와 마찬가지로 현재 열려 있는 캔버스(currentId)는 건드리지 않고 목록에만 새 항목 추가(복제본을 바로 열지 않음). `Sidebar.tsx`의 다이어그램 목록 아이템에 이름 변경과 삭제 사이에 `content_copy` 아이콘 버튼 추가(기존 두 버튼과 동일한 호버 노출·`stopPropagation` 패턴, `aria-label="Duplicate {name}"`). `verify_diagram_duplicate.mjs` e2e 신규 17항목 PASS(호버 시 아이콘 노출·클릭 시 다이얼로그+기본값 prefill 확인·취소 시 목록 불변·확인 시 목록 +1·원본 유지·복제본에 원본 데이터(엔티티) 그대로 존재·복제 중 현재 캔버스/currentId 불변·복제본 열기 후 정상 전환·복제본 삭제해도 원본 유지), 회귀 verify_backend 23/24 PASS(1건은 2026-06-30 문서화된 기존 stale "reload 후 빈 캔버스"로 무관), 빌드 PASS, 스크린샷 확인(ss_diagram_duplicate). manual.html·USER_MANUAL.md 3절(좌측 사이드바) 갱신 |
| 2026-07-07 | 우측 고정 편집 패널 → 온디맨드 편집 모달 전환 | 프론트엔드 | 항상 떠 있는 320px 우측 패널이 브라우저 화면을 잡아먹어 불편하다는 피드백 반영 — 패널을 완전히 제거하고 캔버스를 풀폭으로 전환, 편집은 명시적 트리거(아이콘 클릭·우클릭)로만 모달을 열어 수행하도록 재구성. **선택(하이라이트+Delete 삭제)과 편집 모달 오픈을 별개 상태로 분리**한 것이 핵심: `erdStore.ts`에 `editorOpen: 'entity'\|'relationship'\|'memo'\|null` + `openEntityEditor`/`openRelationshipEditor`/`openMemoEditor`/`closeEditor` 신규, 기존 `selectEntity`/`selectEdge`/`selectMemo`는 하이라이트만 하고 모달에 관여하지 않음. 트리거 3종 — ① 엔티티 헤더의 16px **ⓘ 아이콘**(호버 시 읽기전용 미리보기 팝오버로 설명·전체 컬럼·UQ 배지·서브타입 요약을 노출, 클릭 시 모달), ② 관계선 **선택 시 중간점에 뜨는 ✎ 아이콘**(클릭 시 모달), ③ 엔티티·관계선·메모 공통 **우클릭 컨텍스트 메뉴**(편집/삭제 — 메모는 전용 아이콘이 없어 이 경로가 유일한 모달 진입점, 인라인 textarea 직접편집은 별도로 유지). 신규 `EditorModal.tsx`(기존 DialogModal 오버레이 패턴 재사용, `fixed inset-0 bg-black/60`+중앙 카드 640px, Esc/백드롭 닫기)·`ContextMenu.tsx`(뷰포트 클램핑, 외부클릭/Esc로 자동 닫힘) 신규 추가. `EntityEditPanel`/`RelationshipEditPanel`/`MemoEditPanel`은 내부 편집 로직(필드·버튼·스토어 액션) 전부 그대로 두고 컨테이너만 `<aside>`→`<EditorModal>`로 교체. `App.tsx`는 우측 슬롯 삼항연산자를 걷어내고 세 패널을 각자 `editorOpen` 가드로 자가 렌더링하는 형제로 배치. 구현 중 실제 버그 발견·수정: 메모 노드 textarea의 `onFocus→selectMemo()`가 (당시엔 선택 액션도 `editorOpen:null`을 같이 세팅했었음) 모달이 떠 있는 상태에서 배경 요소가 포커스를 받으면 모달을 조용히 닫아버리는 문제 — 선택 액션에서 `editorOpen` 리셋을 제거하고, 대신 `deleteEntity`/`deleteRelationship`/`deleteMemo`에 "삭제 대상이 현재 열린 모달의 대상일 때만" 정리하는 타겟 클린업을 추가해 해결. `verify_editor_modal.mjs` e2e 신규 18항목 PASS(호버 미리보기 표시/해제, 클릭만으로는 모달 미표시, 모달 폭 640px, 모달 내 편집→노드 반영, ×/우클릭편집/우클릭삭제 전 경로). 우측 패널을 다루던 기존 스크립트 14개 전부 새 트리거(정보 아이콘 클릭·✎ 아이콘 클릭·`page.evaluate`+native MouseEvent로 우클릭)에 맞게 마이그레이션 후 회귀 전부 PASS: verify_features 14·verify_fk_cleanup(ALL)·verify_logical(DONE)·verify_column_drag(ALL)·verify_subtype 22·verify_edge_anchor 15·verify_reltype_preview(ALL)·verify_rel_sidepanel 28·verify_sql_export 14·verify_self_ref 13·verify_memo 17·verify_entity_delete_key 10·verify_relationship_delete_key 14·verify_memo_delete_key 13. 이 과정에서 발견한 사전 결함 2건도 함께 수정: `EntityEditPanel`의 컬럼 "Add"와 서브타입 "Add Subtype" 버튼이 둘 다 `:has-text("Add")`에 매칭되던 선택자 충돌에 `data-testid="add-column"` 추가로 해소, `verify_design.mjs`의 관계 타입 버튼 라벨이 과거 개편 이전 문구로 남아있던 걸 현재 라벨로 갱신. 빌드 PASS, 스크린샷 확인(ss_editor_modal·ss_entity_hover_preview·ss_entity_edit_modal·ss_context_menu). manual.html·USER_MANUAL.md 전면 갱신(1 화면구성/3 사이드바/4.2·4.3·4.4 엔티티/5 컬럼/6.2·6.3 관계/7 서브타입/8.1·8.2·8.3·8.5 메모/9.1 전체화면/13 빠른시작/14 FAQ — "우측 패널"·"속성 패널" 문구 전부 "편집 모달"·아이콘·우클릭 흐름으로 교체, grep으로 잔존 0건 확인). ※ 확인된 기존 무관 stale 2건(수정 범위 밖, 방치): `verify_sidebar_cleanup.mjs`의 "Help 메뉴 유지" 단언은 2026-06-18에 Help 메뉴가 의도적으로 제거된 이후 원래부터 실패하던 항목이고, FK 컬럼명이 상위 PK명을 그대로 쓰는 명명 규칙 특성상 `_id` 접미사를 기대하는 일부 테스트 문자열 단언은 실제 동작과 무관하게 원래부터 어긋나 있었음(둘 다 이번 변경 이전부터 존재, 이번 작업과 무관) |
| 2026-07-07 | 편집 모달 전환 작업을 origin/main과 병합 (협업·다중선택·서브타입 관계·복제 병존) | 프론트엔드+서버 | 위 편집 모달 전환을 로컬 main(4커밋 뒤처짐)에서 진행하는 사이 origin/main에 실시간 협업(링크 공유)·엔티티/메모 다중 선택(러버밴드)·서브타입 관계 연결(스코프 지정+직접 드래그)·다이어그램 복제 4개 기능이 먼저 병합돼 있어, push 전 `git merge origin/main`으로 통합 — `App.tsx`·`ERDCanvas.tsx`·`RelationshipEditPanel.tsx`·`erdStore.ts`·`CLAUDE.md`·`USER_MANUAL.md`·`manual.html`·다수 스크린샷/SQL 샘플에서 충돌 발생(양쪽이 같은 파일의 서로 다른 기능을 동시 수정). 단순 텍스트 union이 아니라 각 충돌을 실제 로직 결합으로 해결 — ① `erdStore.ts`: 내 `editorOpen`(모달 오픈 상태)과 그쪽의 `selectedEntityIds`/`selectedMemoIds`/`setSelection`(다중 선택)을 모두 보존하고, `selectEntity`/`selectEdge`/`selectMemo`가 다중 선택 배열은 초기화하되 `editorOpen`은 건드리지 않도록(모달-포커스 버그 불변식 유지) 병합. ② `ERDCanvas.tsx`: 내 우클릭 컨텍스트 메뉴 핸들러(`onNodeContextMenu`/`onEdgeContextMenu`)와 그쪽의 `onSelectionChange`(러버밴드)를 `<ReactFlow>`에 함께 배선, destructure에서 이미 죽은 코드가 된 내 쪽 `updateNodePosition`/`updateMemoPosition`(그쪽이 `moveNodes`로 통합)을 제거. ③ `RelationshipEditPanel.tsx`: 내 `EditorModal` 컨테이너를 유지하되 내용은 그쪽의 서브타입 스코프 드롭다운(`SubtypeScopeSelect`)·라벨·식별 체크박스 비활성 조건을 그대로 채택(내 쪽 구버전 콘텐츠는 서브타입 인식이 없어 버림). ④ `App.tsx`: `MultiSelectPanel`은 그쪽이 원래 설계대로 `<aside>`(캔버스와 나란히, 선택한 항목이 계속 보여야 하는 UX상 모달로 바꾸면 오히려 나쁨) 그대로 유지하고 내 우측 슬롯 삼항연산자(이미 제거됐어야 할 구코드)만 제거. ⑤ `CLAUDE.md`/문서: 날짜순으로 재정렬, 겹치는 안내문 문구는 자연스럽게 합성. `package.json`(`@fastify/websocket` 등 신규 의존성) 반영 위해 `npm install` 재실행. 병합 후 `tsc -b && vite build` PASS(타입 오류 0), 전체 회귀 재실행 — 내 것 14개 스크립트 + 그쪽 것 5개 스크립트(`verify_share`·`verify_multiselect`·`verify_subtype_relationship`·`verify_subtype_direct_connect`·`verify_diagram_duplicate`) 전부 PASS. 이 과정에서 `verify_subtype_relationship.mjs`·`verify_subtype_direct_connect.mjs`·`verify_backend.mjs` 3개가 모달 전환 이전 구조(우측 상시 패널 가정)로 작성돼 있어 내 트리거 마이그레이션 패턴(정보 아이콘/✎ 아이콘 클릭으로 모달 열기)을 적용해 수정, 추가로 `verify_subtype_direct_connect.mjs`에서 "이미 선택된 엣지의 ✎ 아이콘을 다시 클릭"하는 동일 계열의 재현 버그(이전 Phase C에서도 발견했던 것)를 한 번 더 발견해 루프 진입 전 명시적 캔버스 클릭으로 선택 해제하는 방식으로 수정. 수정 후 3개 스크립트 각각 26·12·23/24 PASS. 서버 회귀(`ALLOW_PGMEM=1 node server/index.js`, 8080)로 `verify_server` 8·`verify_backend` 23/24(1건은 기존 문서화된 stale)·`verify_share` 6·`verify_diagram_duplicate` 6 PASS. ※ 이번에 새로 발견한 기존 무관 stale 1건(수정 범위 밖, 방치): `verify_mcp_connect_ui.mjs`의 "명령에 원격 HTTP 형태" 단언이 커밋 `bea0db5`(`--scope user` 플래그 추가, 세션 재접속 안정성 개선)로 실제 명령 문자열이 늘어난 뒤로 원래부터 실패 중이었음(git blame으로 이번 작업 이전임을 확인, 명령 자체는 정상 동작 — 테스트 문자열만 구버전). 두 기능 세트 모두 무회귀 확인 |
| 2026-07-23 | 캔버스 엔티티 노드 인라인 더블클릭 빠른 편집 | 프론트엔드+문서 | 상세 편집 모달(ⓘ 아이콘)을 열지 않고도 캔버스 상의 엔티티 노드에서 영문 물리명(`entity.name`·`col.name`), 한글 논리명(`entity.logicalName`·`col.logicalName`), 데이터 타입 및 크기(`col.type`+`col.size`, `VARCHAR(200)` 등 자동 파싱)를 **더블클릭**하여 인라인 편집할 수 있도록 구현 — `EntityNode.tsx`(`InlineInput` 컴포넌트, `parseTypeAndSize` 정규식, 더블클릭 이벤트 및 stopPropagation 처리), 서브타입(SubSet) 속성/명칭도 적용. `USER_MANUAL.md`·`public/manual.html` 매뉴얼 4.2절 및 5절에 더블클릭 빠른 편집 설명 반영. E2E 검증 스크립트 `verify_inline_edit.mjs` 신규 작성 |
| 2026-07-23 | 관계선 편집(✎) 버튼 위치 계산 오류 수정 | 프론트엔드 | 선택된 관계선의 ✎ 편집 버튼 위치가 꺾인 엣지 경로(SmoothStepPath)를 반영하지 않고 직선 산술 중점 `((sX+tX)/2, (sY+tY)/2)`으로 계산되어 엣지선과 떨어진 공중 또는 다른 엔티티 노드 중앙에 렌더링되던 버그 수정 — `RelationshipEdge.tsx`에서 `getSmoothStepPath`가 반환하는 실제 엣지선 위의 `labelX`, `labelY` 좌표를 사용하도록 수정. `verify_edge_button_pos.mjs` E2E 테스트 스크립트 작성 |
| 2026-07-30 | 관계선 중간 우회(꺾기) — 선을 드래그해 사이에 놓인 엔티티 피하기 | 프론트엔드 | 사용자 요청 — A·B 사이에 C 엔티티가 있으면 관계선이 그 위를 지나 가려지는데, 기존 끝점 부착 위치 드래그(2026-06-29)로는 해결할 수 없었음. 선을 위/아래로 드래그하면 양쪽 엔티티 쪽 구간이 늘어나며 우회하는 직각 경로 추가. 데이터 모델은 **가산적**: `Relationship.midOffset?: MidOffset`(자동 중간점 기준 **상대** 오프셋이라 엔티티 이동에도 우회 모양 유지, optional이라 기존 저장 파일 호환). 렌더는 `edgeConnection.ts`에 순수 기하 `buildBendRoute`(엔티티 법선으로 24px stub → 우회축까지 → 횡단 → 반대쪽 stub → 진입, `simplify`+`roundedPolyline`로 자동 경로와 같은 8px 라운드 코너) 신설 — `midOffset`이 없으면 기존 `getSmoothStepPath` 그대로라 무회귀. stub 덕에 마지막 구간이 항상 면 법선 방향이어서 까마귀발·식별 막대는 수정 없이 정렬됨. 축은 부모 쪽 연결 면에서 파생(`bendAxis`: 좌/우 면=상하 드래그, 상/하 면=좌우 드래그)하고 커서(`ns-resize`/`ew-resize`)로 표시, 의미 없는 축으로만 끌면 커밋하지 않아 히스토리·협업 op를 남기지 않음. 트리거는 선 전체(히트 path, `nopan`으로 팬·더블클릭 줌 차단) + 선 중앙을 덮는 ✎ 아이콘(3px 임계값으로 제자리 클릭은 편집 모달 유지, 드래그 직후 click은 300ms 억제). 해제는 선/아이콘 더블클릭 또는 관계 편집 모달의 **우회 해제** 버튼(우회 있을 때만 노출). `erdOps.updateRelationshipMidOffset`(순수, FK 부수효과 없음)·`opDispatch` op 등록(실시간 협업)·`erdStore` thin wrapper(`pushHistory('relMid:id')` → Undo 1회). 구현 중 발견·수정한 결함 3건: ① 보이는 선 path들이 투명 히트 path 위에 겹쳐 pointer 이벤트를 가로채 선 정중앙에서만 드래그가 안 먹던 문제(`pointerEvents:'none'` 부여, 클릭 선택은 g로 버블링돼 그대로), ② 캔버스 더블클릭 메모 생성이 `.react-flow__edgelabel-renderer`(✎·끝점 핸들)를 걸러내지 않아 엣지 오버레이 더블클릭 시 엉뚱한 메모가 생기던 **기존 버그**(끝점 핸들 더블클릭에도 해당), ③ 더블클릭의 두 번째 클릭이 ✎ onClick을 발동해 우회 해제와 동시에 편집 모달이 열리던 문제(`e.detail>1` 가드). `verify_edge_bend.mjs` e2e 신규 25항목 PASS(우회 필요 상황 재현→아래/위 우회·장애물 회피 확인·끝점 불변·경로 길이 증가·Q코너 4개·축 제약·클릭 선택 보존·✎ 선 위 위치·아이콘 드래그·영속·Undo/Redo·더블클릭 복귀·메모 미생성·모달 버튼), 회귀 verify_edge_anchor 15·verify_memo 17·verify_rel_sidepanel 28·verify_features 14·verify_self_ref 13·verify_multiselect 23 PASS, 빌드 PASS, 스크린샷 확인(ss_edge_bend). manual.html·USER_MANUAL.md 6.4절을 "관계선 모양 조정 — 끝점 위치·우회 경로"로 확장. ※ 부수 정리: `verify_edge_button_pos.mjs`가 원래부터 실패하던 stale(① 빈 캔버스에서 `.react-flow__node`를 먼저 기다려 항상 타임아웃, ② bbox 중심 force 클릭이 선 위가 아님, ③ 기본 그리드 10px 간격이라 엣지가 노드에 완전히 가려짐)임을 커밋본 대조(`git stash`)로 확인하고 3건 모두 수정해 PASS로 복구 |
| 2026-07-30 | 관계선 우회 드래그 축 판정 수정 (하단/상단 면 부착 시 무반응) | 프론트엔드 | 같은 날 추가한 우회 기능이 드래그 축을 **부모 쪽 연결 면**으로 판정해(`bendAxis(sourcePosition)`), 사용자가 끝점을 하단 면에 붙여 둔 좌우 배치에서는 선이 가로로 달리는데도 축이 x로 잡혀 상/하 드래그가 전혀 먹지 않던 문제 수정 — 축을 **두 끝점의 실제 진행 방향**으로 판정하도록 변경(`bendAxis(s, t)`: 가로 거리 ≥ 세로 거리면 y축=상/하 드래그). 좌우/상하 배치 모두 연결 면과 무관하게 올바른 축이 잡힌다. 추가로 오프셋 0의 기준선을 개선 — 양 끝이 같은 방향으로 나가면(둘 다 하단/상단, 둘 다 좌/우) 바깥쪽 stub을 기준(`baseX`/`baseY`)으로 삼아 자동 경로와 같은 모양에서 드래그가 시작되게 함(기존엔 끝점 y 평균이라 드래그 시작 시 선이 튀었음). `edgeConnection.ts`(bendAxis 시그니처·baseX/baseY)·`RelationshipEdge.tsx`(비우회 상태의 커서/툴팁 축 계산도 같은 함수 사용) 수정. `verify_edge_bend.mjs`에 하단 면 부착 케이스 추가(끝점 2개를 하단 면으로 드래그 → 좌우 배치 확인 → 상/하 드래그로 우회 → 끝점 하단 면 유지) **28항목 PASS**, 회귀 verify_edge_anchor 15·verify_rel_sidepanel 28·verify_features 14·verify_self_ref 13 PASS, 빌드 PASS, 스크린샷 확인(ss_edge_bend_bottom — 하단 면 부착 상태에서 중간 엔티티 아래로 우회). manual.html·USER_MANUAL.md 6.4절에 "연결 면이 아니라 두 엔티티의 배치로 축을 판단" 명시 |
| 2026-09-01 | 한국어/영어 다국어(i18n) 지원 | 프론트엔드+문서 | 한국어 단일 언어였던 UI·가이드를 영어와 함께 지원 — 한국어 기본 + 상단 바 🌐 토글(자동 감지 없음), 선택은 localStorage 영속. **라이브러리 없이 자체 경량 구현**: `src/i18n/`(`locale.ts` zustand persist(`erd-locale`, themeStore 패턴 미러링)·정본 `ko.ts`·`en.ts`·`errors.ts`·`labels.ts`·`index.ts`의 `useT()`/`getT()`/`{name}` 보간) 신규. **타입 안전**: `MessageKey = keyof typeof ko` + `en: Record<MessageKey, string>`이라 번역 누락이 런타임 빈 문자열이 아니라 `tsc -b` 컴파일 에러로 잡힘. **경계 정의가 작업의 핵심** — 번역 대상은 `src/components/**`·`diagramStore`·`sharedSessionStore`·`dialogStore`·`fileIO`·`exportImage`의 화면 문자열(고유 ~170개)이고, **제외**: `server/**`의 `{error:'code'}`(기계 판독용)·`collab-hub`/`collab-routes`의 콘솔 전용 message·`mcp/src/**` 전부(LLM 대면, 로케일 무관해야 함)·`src/types/erd.ts`(`RELATIONSHIP_LABELS`를 MCP 도구 설명이 소비하는 정본이라 무변경, UI만 `relType.*` 키로 별도 조회)·사용자 데이터(논리명·설명·메모·다이어그램명)·erdOps 기본값(`Entity{N}`·`id`·`column`·`SubType{N}`·`SubSet`·`parent_` — 전부 이미 영문). **선행 정리(0단계)**: 삭제 확인 다이얼로그 4종이 App/ERDCanvas/각 편집모달에 3벌씩 복붙돼 있던 것을 `store/deleteActions.ts`로 통합(번역 키 4개로 축소), `core/relationshipSides.ts`의 `labelForSides`가 '식별'·'점선' 조각을 코드로 이어붙여 영어 어순에서 깨지던 것을 `i18n/labels.ts`로 옮기고 조립 순서를 사전(`rel.sideLabel` 템플릿)이 소유하게 재설계 — core에는 순수 로직만 잔류. **에러 문구 중앙화**: `AuthModal`에만 있던 코드→문구 맵을 `i18n/errors.ts`로 승격하고 `not_found`·`login_required`·`invalid_or_revoked_share`·`name_required`·`invalid_token`까지 커버(기존엔 `오류가 발생했습니다 (not_found)`처럼 raw 코드가 노출되던 UX 결함 동시 해소). `server/collab-auth.js`의 `'게스트'`는 서버가 로케일을 모르므로 `label: null`로 바꾸고 클라이언트가 `collab.guest`로 폴백(`protocol.ts` 타입 `string \| null`). **생성 텍스트는 생성 시점 로케일을 따름**(사용자 결정): `diagramStore`의 '새 다이어그램'·'○○ 사본' 프리필과 `exportSql`의 파일 헤더·서브타입 구분자 COMMENT(`generateMySQLDDL`/`buildSubtypeDDL`에 `TFunc` 인자 추가 — 스토어를 import하지 않아 순수성 유지). ※ 조사 중 CLAUDE.md 이력의 "식별 관계 FK 논리명 한글 자동 조합"이 현재 코드와 불일치함을 확인 — `erdOps.ts:115`는 부모 PK 논리명을 그대로 복사할 뿐이라 소급 영향 없음. **가이드 3종 영문판**: 세 페이지가 M3 팔레트 CSS를 각자 복붙(번역본 추가 시 6벌)하던 것을 `public/guide.css`로 공통 추출(`:root` 17개 변수 통합, 공통 24블록) 후 `public/en/{manual,mcp-guide,prompt-guide}.html` 신규 — prompt-guide의 한국어 예시 프롬프트 55개는 직역이 아니라 영문 프롬프트로 재작성. 사이드바/McpConnectModal의 가이드 링크를 `guideBase`로 로케일 분기, 각 페이지 목차에 🌐 상호 링크. **e2e 무회귀 장치**: 사전의 한국어 값을 기존 UI와 글자 단위로 동일하게 유지 + 기본 로케일 `ko` → 38개 스크립트가 쓰는 한글 셀렉터 122곳이 **무수정 통과**. `verify_i18n.mjs` 신규 26항목 PASS(토글·`<html lang>`·localStorage 영속·영어 모드에서도 `Entity1`/`id` 불변·SQL 헤더 양방향·삭제 다이얼로그 양방향·`/en/*.html`이 SPA fallback이 아닌 실제 파일로 서빙되는지). 회귀 dev: verify_features 14·verify_editor_modal 18·verify_entity_delete_key 10·verify_relationship_delete_key 14·verify_memo_delete_key 13·verify_rel_sidepanel 28·verify_memo 17·verify_multiselect 23·verify_fk_cleanup ALL PASS. 회귀 prod(8080): verify_server 8·verify_share 18·verify_diagram_duplicate·verify_gnb_cleanup·verify_sidebar_toggle ALL PASS, verify_backend은 기존 stale 1건(`reload 후 캔버스는 빈 상태`)만 실패로 HEAD와 동일함을 `git stash` 대조로 확인. 빌드 PASS, lint는 기존 baseline 16건 그대로(신규 0). 스크린샷 확인(ss_i18n_en·ss_i18n_ko·ss_i18n_guide_en_{manual,mcp,prompt}·ss_guide_ko_*). manual.html·USER_MANUAL.md에 15절 '화면 언어' 신설 + 상단 바 표에 언어/테마 행 추가, 2026-06-18에 제거된 Help 메뉴를 아직 안내하던 stale 문구 정정. ※ 기존 stale 재확인(무관·방치): `verify_subtype` 2건·`verify_sql_export` 1건은 커밋본에서도 동일 실패, `verify_mcp_guide`는 Help 메뉴 제거 이후, `verify_mcp_connect_ui` 1건은 `--scope user` 추가 이후 원래부터 실패 |
| 2026-09-03 | 엔티티 노드 가독성 개선 (작은 화면 100% 편집 기준) | 프론트엔드 | 17테이블·350컬럼(`hu-commerce`) 실사용 규모에서 작은 노트북 100% 배율 편집 시 한 화면에 들어오는 엔티티가 너무 적던 문제. **조사 중 근본 원인 발견**: Google Fonts의 `.material-symbols-outlined { font-size:24px }`는 캐스케이드 **레이어 밖**인데 Tailwind v4 유틸리티는 `@layer utilities` **안**이라, 컴포넌트에 붙인 `text-[14px]` 류가 전부 무시되고 **앱의 아이콘 28개가 모두 24px로 렌더**되고 있었다(툴바·사이드바 포함 전역). 이것이 컬럼 행 높이를 32px로 고정하던 직접 원인. `index.css`에 같은 '레이어 밖' 규칙으로 실제 사용 중인 10개 크기(11~48px)만 되돌려 **컴포넌트 무수정으로 전역 해결**. 이어서 ① 컬럼 행을 `py-1`→`py-0.5 leading-[15px]`로 압축(서브타입 박스 행 `py-0.5`와 밀도 통일) → **행 32px→19px(−41%)**, ② `--color-node-border` 다크 `#334155`→`#64748b`·라이트 `#b0a8d0`→`#8b82ab`로 **대비 1.8:1→4.0:1**(UI 경계 권장 3:1 충족), ③ 컬럼 타입 `opacity-70` 제거, NOT NULL `*`를 PK와 같은 주황(`text-pk-color`)에서 중립색으로 분리, 논리명 플레이스홀더 `opacity-40`→`60`. ④ **엔티티 접기/펼치기** 신규 — 헤더 우측 chevron(`entity-collapse-toggle`) + 접힘 시 컬럼 수 배지(`entity-column-count`) + 줌 툴바 일괄 토글(`collapse-all-toggle`). 접힘은 **저장 포맷이 아닌 UI 상태**(`erdStore.collapsedEntityIds`)라 `types/erd.ts`·MCP·협업 op·저장 파일을 전혀 건드리지 않는다(다이어그램 전환 시 초기화). 접혀도 핸들·`entity-info-icon`·선택·삭제가 살아 있어 연결·편집 경로가 보존된다(17개 e2e가 쓰는 info 아이콘 유지가 설계 제약). **정량 결과**(1440×900·100%, `git stash` 대조 실측): 세로 900px에 통째로 담기는 테이블 **14/17→17/17**, 전체 노드 높이 합 **10,096px→7,636px(−24%)**, 모두 접으면 **697px**·Fit View 줌 **20%→38%**. `verify_node_density.mjs` 신규 **17항목 PASS**(아이콘 크기 회귀 방지·행 높이·대비비 계산·개별/일괄 접기·접힘 상태 핸들 8개·배지·복원), 회귀 15종 PASS(verify_edge_bend 33·verify_multiselect 23·verify_rel_sidepanel 28·verify_features 14·verify_editor_modal 18·verify_i18n 26·verify_memo 17·verify_edge_anchor 15·verify_self_ref 13·verify_entity_position 5·verify_entity_hover_info 9·verify_edge_button_pos·verify_column_drag·verify_fk_cleanup·verify_handle_size). 빌드 PASS, 다크·라이트 스크린샷 확인. manual.html·USER_MANUAL.md·en/manual.html에 4.5절 '엔티티 접기/펼치기' 신설(기존 4.4→4.6) + 줌 툴바 표 갱신. ※ **부수 정리**: `verify_inline_edit.mjs`가 결함 3개로 **한 번도 통과한 적 없던 것**을 수정해 처음 PASS — ① 빈 캔버스에서 `.react-flow__node`를 먼저 기다려 항상 타임아웃(2026-07-30 `verify_edge_button_pos`와 동일 부류), ② 셀렉터 `input`이 숨은 `input[type=file]`에 매칭, ③ `page.keyboard.fill`은 Playwright에 없는 API. ※ **범위 밖(의도적)**: 줌 기반 LOD(축소가 문제가 아니라는 사용자 확인 + `verify_subtype:114-133`의 말줄임 금지 단언과 충돌), 노드 폭 상한·말줄임(현행 유지 결정), 큰 테이블 max-height+내부 스크롤(접기가 더 낮은 위험으로 같은 문제 해결), 우클릭 메뉴 접기 항목(엔티티 전용 액션을 공용 `ContextMenu`에 넣는 결합 회피), `EntityNode:86`의 selector 없는 전체 스토어 구독(모든 `set()`에 전 노드 리렌더 — 실제 성능 이슈지만 별건), 서브타입 이름 인라인 편집이 `handleFieldSubmit`에 분기가 없어 조용히 버려지는 버그(별건). ※ 기존 stale 유지: `verify_subtype` 2건(커밋본에서도 동일 실패) |
| 2026-09-03 | 공유 링크 — 로그인 없이 열람 가능 | 백엔드+프론트엔드 | 링크를 받은 사람이 반드시 로그인해야 볼 수 있던 제약 해제(사용자 요청). **익명 경로는 이미 구현돼 있었고 `ALLOW_ANON_SHARE` 환경변수로 잠겨만 있던 상태**라(2026-07-02 1단계 MVP의 보수적 기본값), 기본값을 뒤집고 잠금 스위치를 `REQUIRE_SHARE_LOGIN=1`로 반전시켰다. ① `server/share-routes.js`의 `GET /api/shared/:token` 401 게이트 제거 — 토큰 자체가 그 다이어그램 **하나**에 대한 읽기 capability다. ② `server/access-util.js`의 `resolveDiagramAccess`가 비로그인에게 게스트 신원(`guestId`)을 발급하도록 기본 허용. 단 **익명은 `viewer` 링크만** 허용하고 `editor` 링크는 로그인을 계속 요구한다 — 익명 편집자는 `actorId`가 매번 새 게스트라 누가 고쳤는지 남지 않기 때문(사용자 요청은 '볼 수 있게'였으므로 쓰기는 열지 않았다). ③ `src/store/sharedSessionStore.ts`의 `enter()`에서 '비로그인이면 로그인 모달 강제' 조기 반환 제거 — 401 catch는 `REQUIRE_SHARE_LOGIN=1` 잠금 인스턴스용 폴백으로 남긴다. 서버가 보내는 참여자 `label`은 비로그인일 때 null이고 클라이언트가 `collab.guest`로 폴백하는 경로(2026-09-01 i18n 작업분)가 그대로 동작해 게스트 아바타가 정상 표시됨을 확인. **불변식 유지**: 소유자 전용 `/api/diagrams/:id`는 익명에게 여전히 401, 토큰↔다이어그램 바인딩 유지, 폐기 토큰 403, 링크 진입자는 읽기 전용. `verify_share.mjs`의 '익명 401' 단언을 뒤집고 **익명 브라우저 시나리오 6항목 신규 추가**(로그인 모달 미노출·다이어그램 렌더·읽기전용 배지·Add Entity 없음·협업 live 게스트 참여·API 200) → **24항목 ALL PASS**. `REQUIRE_SHARE_LOGIN=1`로 띄워 익명 401 잠금도 별도 확인. 회귀 verify_server 8·verify_diagram_duplicate·verify_features 14·verify_i18n 26·verify_node_density 17 PASS, verify_backend은 기존 stale 1건(`reload 후 캔버스는 빈 상태`, 2026-06-30 문서화)만 실패로 무관. 빌드 PASS, lint 기존 baseline 16건 유지. 사용자 대면 문구 갱신 — ShareModal 안내(`share.intro1` ko/en), manual.html·USER_MANUAL.md·en/manual.html 10.3절(제목의 '(로그인 필요)' 제거 + 본문), README 환경변수 표·`.env.example`에 `REQUIRE_SHARE_LOGIN` 추가. 스크린샷 확인(ss_share_anon — 익명 세션에서 공유 세션 표시·읽기 전용 배지·게스트 아바타). ※ **부수 정리**: `verify_share.mjs`의 스크린샷 경로 3곳이 Windows 절대경로(`C:/project/...`)로 하드코딩돼 macOS에서 돌릴 때마다 리포 안에 `C:/project/harness-test/...` 디렉토리가 생성되던 것을 상대 경로로 수정. ※ **보안 영향(의도된 것)**: 링크를 아는 사람은 누구나 로그인 없이 그 다이어그램을 읽을 수 있다. 토큰이 URL 경로(`/d/<token>`)에 있어 브라우저 히스토리·리퍼러·nginx 액세스 로그에 남으므로, 링크 유출이 곧 열람 허용이다(폐기는 공유 모달에서 즉시 가능). 사내 전용 인스턴스는 `REQUIRE_SHARE_LOGIN=1`로 예전 동작 복구 |
